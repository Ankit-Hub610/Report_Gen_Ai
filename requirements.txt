streamlit>=1.38  # PINNED (min version) — a reasonably recent Streamlit is needed
                 # for extra_streamlit_components (the "stay logged in on
                 # refresh" cookie, see app.py: CookieManager) to work reliably.
Pillow  # explicit — used to resize/recompress branding images (logo, login wallpaper)
        # before storage, so the whole branding blob stays small enough to sync
        # reliably via GitHub (see app.py: _resize_for_branding). Streamlit already
        # depends on Pillow transitively, but pinning it here is more robust than
        # relying on that indirect dependency staying true forever.
extra-streamlit-components>=0.1.60  # PINNED (min version) — CookieManager: real browser cookie for "stay logged in
                             # on refresh" (see app.py: _set_session_cookie /
                             # _get_session_cookie). Replaced an earlier hand-rolled
                             # components.html(<script>document.cookie=...) version
                             # that was unreliable (race with Streamlit's own
                             # re-render tearing the injected iframe down before its
                             # script ran, so the cookie sometimes never got written).
duckdb        # SQL query engine (Phase 5) — real SELECT/WHERE/GROUP BY over the loaded dataset
pandas
numpy
plotly==6.8.0  # PINNED — was unpinned before, so Streamlit Cloud silently installed
               # whatever the newest Plotly release happened to be on every redeploy.
               # A newer release changed how the library validates styling calls
               # (see chart_engine.py build_figure), which is what caused the
               # "ValueError" crash on every Line/Area chart. Pinning stops that
               # from happening again on a future redeploy.
reportlab
qrcode        # generates the scannable UPI QR code on the 💎 Plans page's "Pay via UPI" flow
openpyxl      # needed to read/write .xlsx files (pd.ExcelFile)
python-docx   # Word (.docx) export of the Full Analysis report (see modules/report_export.py)
kaleido==0.2.1  # PINNED — newer kaleido (1.x) needs a headless Chrome browser that
                   # isn't available on Streamlit Cloud by default, so fig.to_image()
                   # was silently failing and every chart was disappearing from the
                   # exported PDF. 0.2.1 is the old self-contained static-binary
                   # version that just works, no Chrome needed.
pdfplumber    # optional: only needed if you upload PDFs as a data source on Raw Analysis
requests      # needed for the 🤖 AI Assistant page (talks to the free OpenRouter API)
sqlalchemy             # 🔌 Database Connector tab — connect to an external SQL database
psycopg2-binary        # PostgreSQL driver for the Database Connector
pymysql                # MySQL / MariaDB driver for the Database Connector
streamlit-autorefresh  # powers the auto-refresh toggle on Database Connector query tabs
gspread                # 🔗 Google Sheet tab (Private/Service Account mode) — reads a private sheet's rows
google-auth             # authorizes the service-account JSON key used by gspread above
PyMuPDF                # renders a PDF's first page to PNG for the Settings -> App Branding
                        # "Login page logo" upload (only needed if someone uploads a PDF logo;
                        # PNG/JPG logos work without it)
streamlit-mic-recorder  # 🎤 voice assistant push-to-talk button (Full Analysis, Business
                        # Insights, Boss Dashboard) - records + transcribes via the browser's
                        # own speech recognition, no API key needed for this part
